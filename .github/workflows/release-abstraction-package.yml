name: release-abstraction-package

on:
  push:
    branches: [main]
    paths:
      - "src/Ashampoo.Translation.Systems.Formats.Abstractions/**"

jobs: 
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          
      - name: Get Version
        id: GetVersion
        uses: ./.github/workflows/reuse-generate-version